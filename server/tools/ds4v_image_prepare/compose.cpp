#include "compose.h"
Composition compose(const nlohmann::json &,dflash::common::Tokenizer &,
                    const dflash::vision::ImagePromptLimits &,const std::string &,const std::string &) {
    Composition result;
    result.error="not implemented";
    return result;
}
